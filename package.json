{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc && webpack --mode production"
  },
  "devDependencies": {
    "typescript": "4.7.4"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
